export enum EFirebaseOperators {
  EQUALS = '==',
  DIFF = '!=',
  LESSER = '<',
  LESSEQUAL = '<=',
  GREATER = '>',
  GREATEQUAL = '>=',
  IN = 'in',
  NOTIN = 'not-in',
  ARRCONTAINS = 'array-contains',
  ARRCONTAINSANY = 'array-contains-any',
}
